name: Build GKI Kernel with KernelSU (backslashxx)

on:
  workflow_dispatch:
    inputs:
      force_clean_build:
        description: 'Force clean build (ignore cache)'
        required: false
        default: false
        type: boolean
      enable_telegram:
        description: 'Enable Telegram notifications'
        required: false
        default: true
        type: boolean
      create_release:
        description: 'Create GitHub Release with artifacts'
        required: false
        default: true
        type: boolean
      debug:
        description: 'Enable debug output'
        required: false
        default: false
        type: boolean

permissions:
  contents: write
  packages: write

env:
  CCACHE_DIR: ${{ github.workspace }}/.ccache
  TOOLCHAIN_DIR: ${{ github.workspace }}/toolchains

jobs:
  setup:
    runs-on: ubuntu-latest
    outputs:
      force_clean: ${{ steps.config.outputs.force_clean }}
      build_start_time: ${{ steps.timestamp.outputs.start_time }}
      create_release: ${{ steps.config.outputs.create_release }}
    steps:
      - name: Record Build Start Time
        id: timestamp
        run: |
          echo "WORKFLOW_START_TIME=$(date +%s)" >> $GITHUB_ENV
          START_TIME=$(date +%s)
          echo "start_time=$START_TIME" >> $GITHUB_OUTPUT
          echo "::notice::Workflow started at $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
      - name: Set Build Config
        id: config
        run: |
          echo "force_clean=${{ github.event.inputs.force_clean_build }}" >> $GITHUB_OUTPUT
          echo "create_release=${{ github.event.inputs.create_release }}" >> $GITHUB_OUTPUT

  build:
    needs: setup
    runs-on: ubuntu-latest
    timeout-minutes: 180
    
    steps:
      - name: Free Disk Space & Install Build Dependencies
        run: |
          echo "=== Disk before cleanup ==="
          df -h /
          sudo apt-get purge -yq \
            '^aspnetcore-.*' '^dotnet-.*' '^llvm-.*' \
            'php.*' '^mongodb-.*' '^mysql-.*' \
            azure-cli google-cloud-cli google-chrome-stable \
            firefox powershell mono-devel libgl1-mesa-dri \
            'ghc-*' 'cabal-install' \
            || true
          sudo rm -rf \
            /usr/share/dotnet \
            /usr/local/lib/android \
            /opt/ghc \
            /opt/hostedtoolcache/CodeQL \
            /usr/local/share/powershell \
            /usr/share/swift \
            /usr/local/.ghcup \
            /usr/lib/jvm \
            /usr/local/graalvm \
            "$AGENT_TOOLSDIRECTORY"
          docker image prune -af 2>/dev/null || true
          sudo apt-get autoremove -yq
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*
          echo "=== Disk after cleanup ==="
          df -h /
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends \
            bc bison flex libssl-dev libelf-dev \
            build-essential binutils-aarch64-linux-gnu \
            python3 python3-pip rsync \
            ccache curl zip unzip git \
            libncurses-dev gawk openssl \
            dwarves zstd pahole
          echo "✅ Dependencies installed"
      - name: Restore ccache
        uses: actions/cache@v4
        with:
          path: ${{ env.CCACHE_DIR }}
          key: ccache-backslashxx-${{ github.ref_name }}-${{ github.run_id }}
          restore-keys: |
            ccache-backslashxx-${{ github.ref_name }}-
            ccache-backslashxx-
      - name: Configure ccache
        run: |
          ccache --max-size=5G
          ccache --set-config=compression=true
          ccache --set-config=compression_level=1
          echo "ccache version: $(ccache --version | head -1)"
      - name: Download & Verify Clang Toolchain
        run: |
          mkdir -p $HOME/clang
          cd $HOME/clang
          wget -q https://github.com/ZyCromerZ/Clang/releases/download/22.0.0git-20250928-release/Clang-22.0.0git-20250928.tar.gz -O clang.tar.gz
          tar -xzf clang.tar.gz
          rm clang.tar.gz
          echo "✅ $($HOME/clang/bin/clang --version | head -1)"
          echo "CLANG_BIN=$HOME/clang/bin" >> $GITHUB_ENV
          echo "$HOME/clang/bin" >> $GITHUB_PATH
      - name: Download & Verify AArch64 GNU Toolchain
        run: |
          mkdir -p $HOME/toolchains
          cd $HOME/toolchains
          wget -q https://developer.arm.com/-/media/Files/downloads/gnu/14.3.rel1/binrel/arm-gnu-toolchain-14.3.rel1-x86_64-aarch64-none-linux-gnu.tar.xz -O tc.tar.xz
          tar -xJf tc.tar.xz
          rm tc.tar.xz
          TC_DIR=$(ls -d arm-gnu-toolchain-*aarch64*)
          echo "ARM64_TOOLCHAIN=$HOME/toolchains/$TC_DIR/bin/aarch64-none-linux-gnu-" >> $GITHUB_ENV
          echo "✅ AArch64 toolchain ready"
      - name: Download & Verify ARM32 GNU Toolchain
        run: |
          mkdir -p $HOME/toolchains
          cd $HOME/toolchains
          wget -q https://developer.arm.com/-/media/Files/downloads/gnu/14.3.rel1/binrel/arm-gnu-toolchain-14.3.rel1-x86_64-arm-none-eabi.tar.xz -O tc-arm.tar.xz
          tar -xJf tc-arm.tar.xz
          rm tc-arm.tar.xz
          TC_DIR=$(ls -d arm-gnu-toolchain-*arm-none-eabi*)
          echo "ARM32_TOOLCHAIN=$HOME/toolchains/$TC_DIR/bin/arm-none-eabi-" >> $GITHUB_ENV
          echo "✅ ARM32 toolchain ready"
      - name: Pre-Clean Build Artifacts
        run: |
          echo "🧹 Cleaning previous build artifacts..."
          rm -rf AnyKernel3 release-assets changelogs || true
      - name: Checkout Workflow Repo
        uses: actions/checkout@v4
        with:
          clean: false
      - name: Smart Sync Kernel Source
        run: |
          echo "📦 Smart syncing kernel source..."
          if [ -d "kernel/.git" ]; then
            echo "♻️ Existing Kernel tree found. Updating..."
            cd kernel
            git fetch --depth=1 origin android14-6.1-2026-03
            git reset --hard origin/android14-6.1-2026-03
            git clean -fdx
            cd ..
          else
            echo "📥 Downloading Kernel source..."
            git clone --depth=1 --branch=android14-6.1-2026-03 https://android.googlesource.com/kernel/common kernel
          fi
      - name: Store Commit Info
        run: |
          cd kernel
          read -r K_HASH K_SHORT K_DATE K_MSG << EOF
          $(git log -1 --format="%H %h %cd %s" --date=format:'%Y-%m-%d %H:%M:%S')
          EOF
          echo "KERNEL_COMMIT_SHORT=$K_SHORT" >> $GITHUB_ENV
          echo "KERNEL_COMMIT_MSG=$K_MSG" >> $GITHUB_ENV
      - name: Show CCACHE Status
        run: |
          export CCACHE_DIR="${{ env.CCACHE_DIR }}"
          ccache -s | grep -E "(Cacheable calls|Hits|Misses|Hit rate|cache size)" || true
      - name: Setup KernelSU (backslashxx)
        run: |
          cd kernel
          rm -rf KernelSU drivers/kernelsu
          
          # Clone backslashxx/KernelSU from master branch
          git clone --depth=1 --branch=master https://github.com/backslashxx/KernelSU.git KernelSU
          
          # Symlink KernelSU
          ln -sf ../KernelSU/kernel drivers/kernelsu
          
          # Add KSU to kernel makefiles
          grep -q "kernelsu" drivers/Makefile || echo 'obj-$(CONFIG_KSU) += kernelsu/' >> drivers/Makefile
          grep -q "kernelsu" drivers/Kconfig || sed -i '/endmenu/i source "drivers/kernelsu/Kconfig"' drivers/Kconfig
          
          echo "KSU_REPO=backslashxx/KernelSU" >> $GITHUB_ENV
          echo "KSU_BRANCH=master" >> $GITHUB_ENV
      - name: Get KSU Version Info
        run: |
          cd kernel/KernelSU
          git fetch --unshallow --quiet 2>/dev/null || true
          
          KSU_COUNT=$(git rev-list --count HEAD)
          KSU_VERSION=$((KSU_COUNT + 30000))
          
          read -r KSU_C_HASH KSU_C_SHORT KSU_C_DATE KSU_C_MSG KSU_C_AUTH << EOF
          $(git log -1 --format="%H %h %cd %s %an" --date=format:'%Y-%m-%d %H:%M:%S')
          EOF
          
          echo "KSU_VERSION=$KSU_VERSION" >> $GITHUB_ENV
          echo "KSU_COMMIT_SHORT=$KSU_C_SHORT" >> $GITHUB_ENV
          echo "KSU_COMMIT_MSG=$KSU_C_MSG" >> $GITHUB_ENV
      - name: Generate Changelog
        if: needs.setup.outputs.create_release == 'true'
        run: |
          mkdir -p changelogs
          CHANGELOG_FILE="changelogs/CHANGELOG-backslashxx.md"
          
          cat << EOF > "$CHANGELOG_FILE"
          # Kernel Build Changelog
          
          **Variant:** backslashxx (KernelSU)
          **Build Number:** #${{ github.run_number }}
          **KernelSU Version:** r${{ env.KSU_VERSION }}
          
          ---
          ## Component Versions
          | Component | Commit | Summary |
          |-----------|--------|---------|
          | **KernelSU** | \`${{ env.KSU_COMMIT_SHORT }}\` | ${{ env.KSU_COMMIT_MSG }} |
          | **Kernel** | \`${{ env.KERNEL_COMMIT_SHORT }}\` | ${{ env.KERNEL_COMMIT_MSG }} |
          
          ---
          EOF
          
          echo "CHANGELOG_FILE=$CHANGELOG_FILE" >> $GITHUB_ENV
      - name: Apply Source Fixes
        run: |
          cd kernel
          
          # Bypass GKI ABI symbol checks
          rm -f android/abi_gki_protected_exports_* || true
          
          # CLIDR uninitialized fix
          curl -LSs "https://raw.githubusercontent.com/infectedmushi/kernel_patches/main/fix-clidr-uninitialized.patch" -o /tmp/fix-clidr.patch
          patch -p1 --forward < /tmp/fix-clidr.patch 2>/dev/null || true
          
          # Fix localversion script appending dirty flags
          sed -i '/# Check for uncommitted changes\./,/fi/d' ./scripts/setlocalversion 2>/dev/null || true
          
          # Fix libbpf compilation error
          sed -i 's/next_path = strchr(s, '"'"':'"'"');/next_path = (char *)strchr(s, '"'"':'"'"');/' tools/lib/bpf/libbpf.c 2>/dev/null || true
      - name: Setup atp4pixel Config Pillars
        working-directory: kernel
        run: |
          DEFCONFIG="arch/arm64/configs/gki_defconfig"
          
          # Remove existing CONFIG_KSU line if present
          sed -i '/^CONFIG_KSU=/d' "$DEFCONFIG"
          echo "" >> "$DEFCONFIG"

          cat <<EOF >> "$DEFCONFIG"
          # 抹平内核版本后缀，维持原厂 vermagic 兼容性
          CONFIG_LOCALVERSION=""
          CONFIG_LOCALVERSION_AUTO=n

          # 强制关闭模块符号版本与签名校验（允许加载原厂 vendor 分区驱动）
          CONFIG_MODVERSIONS=n
          CONFIG_MODULE_FORCE_LOAD=y
          CONFIG_MODULE_SIG=n
          CONFIG_MODULE_SIG_FORCE=n
          CONFIG_MODULE_SIG_ALL=n

          # KernelSU 核心配置
          CONFIG_KSU=y
          CONFIG_KSU_EXTRAS=y
          CONFIG_KSU_HACK_ARM64_BRANCH_LINK=y
          CONFIG_KSU_LSM_SECURITY_HOOKS=y

          # 网络与系统增强组件 (atp4pixel Pillars)
          CONFIG_NETFILTER_ADVANCED=y
          CONFIG_IP_NF_TARGET_TTL=y
          CONFIG_IP6_NF_TARGET_HL=y
          CONFIG_IP6_NF_MATCH_HL=y
          CONFIG_BPF_STREAM_PARSER=y
          CONFIG_NETFILTER_XT_MATCH_ADDRTYPE=y
          CONFIG_NETFILTER_XT_TARGET_TPROXY=y
          CONFIG_NETFILTER_XT_TARGET_REDIRECT=y
          CONFIG_NETFILTER_XT_MATCH_SOCKET=y
          CONFIG_NETFILTER_XT_MATCH_MARK=y
          CONFIG_NETFILTER_XT_MATCH_IPRANGE=y
          CONFIG_NETFILTER_XT_MATCH_OWNER=y
          CONFIG_NETFILTER_XT_MATCH_MAC=y
          CONFIG_NETFILTER_XT_MATCH_CONNTRACK=y
          CONFIG_NETFILTER_XT_MARK=y
          CONFIG_NETFILTER_XT_CONNMARK=y
          CONFIG_NETFILTER_XT_TARGET_CONNMARK=y
          CONFIG_IP6_NF_NAT=y
          CONFIG_IP6_NF_TARGET_REDIRECT=y
          CONFIG_IP6_NF_TARGET_MASQUERADE=y
          CONFIG_IP_MULTIPLE_TABLES=y
          CONFIG_IPV6_MULTIPLE_TABLES=y
          CONFIG_XFRM_ALGO=y
          CONFIG_INET_IPCOMP=y
          CONFIG_INET_XFRM_TUNNEL=y
          CONFIG_INET_ESP=y
          CONFIG_XFRM_USER=y
          CONFIG_XFRM_SUB_POLICY=y
          CONFIG_INET_XFRM_MODE_TUNNEL=y
          CONFIG_XFRM_STATISTICS=y
          CONFIG_IP_SET=y
          CONFIG_NETFILTER_XT_SET=y
          CONFIG_IP_SET_MAX=65534
          CONFIG_IP_SET_HASH_IP=y
          CONFIG_IP_SET_HASH_NET=y
          CONFIG_TCP_CONG_ADVANCED=y
          CONFIG_TCP_CONG_BBR=y
          CONFIG_NET_SCH_FQ=y
          CONFIG_DEFAULT_BBR=y
          CONFIG_SECURITY_NETWORK=y
          CONFIG_CGROUPS=y
          CONFIG_CGROUP_BPF=y
          CONFIG_CGROUP_SCHED=y
          CONFIG_INOTIFY_USER=y
          CONFIG_TMPFS_POSIX_ACL=y
          CONFIG_TMPFS_XATTR=y

          # Android 14 核心要求的 BTF 调试信息开关（防止 bpfloader 崩溃卡开机）
          CONFIG_DEBUG_INFO=y
          CONFIG_DEBUG_INFO_BTF=y
          CONFIG_DEBUG_INFO_REDUCED=n
          CONFIG_DEBUG_INFO_COMPRESSED_NONE=y

          # 优化选项（与 BTF 完美共存的 ThinLTO 模式）
          CONFIG_LTO_CLANG_THIN=y
          CONFIG_LTO_CLANG_FULL=n
          CONFIG_LTO_NONE=n
          EOF

          # Disable conflicting TCP congestion controls
          sed -i 's/CONFIG_TCP_CONG_BIC=y/CONFIG_TCP_CONG_BIC=n/' "$DEFCONFIG"
          sed -i 's/CONFIG_TCP_CONG_WESTWOOD=y/CONFIG_TCP_CONG_WESTWOOD=n/' "$DEFCONFIG"
          
          # Disable WERROR to avoid build failures from warnings
          echo "CONFIG_WERROR=n" >> "$DEFCONFIG"
      - name: Configure Kernel
        run: |
          cd kernel
          export ARCH=arm64
          export CROSS_COMPILE="${{ env.ARM64_TOOLCHAIN }}"
          export CROSS_COMPILE_COMPAT="${{ env.ARM32_TOOLCHAIN }}"
          
          # Regenerate config with all settings
          make ARCH=arm64 LLVM=1 LLVM_IAS=1 LD=ld.lld O=out gki_defconfig
          make ARCH=arm64 LLVM=1 LLVM_IAS=1 LD=ld.lld O=out olddefconfig
          
          # Verify critical configs are set
          echo "🔍 Verifying kernel configuration..."
          grep -E "CONFIG_KSU|CONFIG_DEBUG_INFO_BTF|CONFIG_LTO_CLANG_THIN" out/.config || true
      - name: Build Kernel
        run: |
          cd kernel
          export ARCH=arm64
          export CROSS_COMPILE="${{ env.ARM64_TOOLCHAIN }}"
          export CROSS_COMPILE_COMPAT="${{ env.ARM32_TOOLCHAIN }}"
          export CCACHE_DIR="${{ env.CCACHE_DIR }}"
          export CC="ccache clang"
          export CXX="ccache clang++"
          
          MAKE_JOBS=$(nproc)
          START_TIME=$SECONDS
          
          if [ "${{ needs.setup.outputs.force_clean }}" = "true" ]; then
            make -j$MAKE_JOBS LLVM=1 LLVM_IAS=1 LD=ld.lld O=out clean
          fi
          
          make -j$MAKE_JOBS \
            LLVM=1 LLVM_IAS=1 \
            ARCH=arm64 \
            CLANG_TRIPLE=aarch64-linux-gnu- \
            CROSS_COMPILE="${{ env.ARM64_TOOLCHAIN }}" \
            CROSS_COMPILE_COMPAT="${{ env.ARM32_TOOLCHAIN }}" \
            CC="$CC" \
            KCFLAGS="-fno-stack-protector" \
            LD=ld.lld \
            HOSTLD=ld.lld \
            O=out
          
          echo "BUILD_TIME=$((SECONDS - START_TIME))s" >> $GITHUB_ENV
      - name: Package Kernel (AnyKernel3)
        run: |
          cd kernel
          KERNEL_VERSION=$(grep "^VERSION" Makefile | cut -d' ' -f3).$(grep "^PATCHLEVEL" Makefile | cut -d' ' -f3).$(grep "^SUBLEVEL" Makefile | cut -d' ' -f3)
          cd ..
          
          git clone --depth=1 --branch=KernelSU https://github.com/deepongi-labs/AnyKernel3-p8a AnyKernel3 || \
          git clone --depth=1 --branch=main https://github.com/deepongi-labs/AnyKernel3-p8a AnyKernel3
          
          cp kernel/out/arch/arm64/boot/Image AnyKernel3/
          [ -f kernel/out/arch/arm64/boot/dtb ] && cp kernel/out/arch/arm64/boot/dtb AnyKernel3/
          
          cd AnyKernel3 && rm -rf .git .github
          ZIP_NAME="AK3-${KERNEL_VERSION}-backslashxx-r${{ env.KSU_VERSION }}.zip"
          zip -r9 "../$ZIP_NAME" .
          cd ..
          
          echo "KERNEL_ZIP=$ZIP_NAME" >> $GITHUB_ENV
      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: kernel-backslashxx-${{ github.run_number }}
          path: |
            ${{ env.KERNEL_ZIP }}
            ${{ env.CHANGELOG_FILE }}

  create_release:
    needs: [setup, build]
    runs-on: ubuntu-latest
    if: always() && needs.setup.outputs.create_release == 'true' && needs.build.result != 'cancelled'
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Download All Artifacts
        uses: actions/download-artifact@v4
        with:
          path: release-assets
      - name: Create GitHub Release
        run: |
          cd release-assets
          find . -type f -exec mv {} . \; 2>/dev/null || true
          
          if ! ls *.zip 1> /dev/null 2>&1; then
            echo "❌ No ZIP artifacts found! Skipping release."
            exit 0
          fi
          
          RELEASE_NOTES="**Build Date:** $(date)
          **KernelSU:** backslashxx (master branch)
          
          ### Features (atp4pixel Pillars)
          - ✅ Full KernelSU support with LSM hooks
          - ✅ BTF debug info for Android 14 bpfloader compatibility
          - ✅ Advanced networking: BBR, IP sets, Netfilter
          - ✅ Module signing disabled for vendor driver compatibility
          - ✅ ThinLTO optimizations
          
          ### Supported Devices
          - Pixel 8, 8a, 8 Pro
          
          Flash ZIP in Kernel Flasher or extract Image to patch boot.img manually."
          
          gh release create "build-${{ github.run_number }}" \
            --title "KernelSU Kernel Build #${{ github.run_number }} (backslashxx)" \
            --notes "$RELEASE_NOTES" \
            --repo "${{ github.repository }}" \
            ./*.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  notify:
    needs: [setup, create_release, build]
    runs-on: ubuntu-latest
    if: always() && github.event.inputs.enable_telegram == 'true'
    steps:
      - name: Send Telegram Notification
        run: |
          STATUS="${{ needs.build.result }}"
          ICON="✅"
          [ "$STATUS" != "success" ] && ICON="⚠️"
          [ "$STATUS" == "failure" ] && ICON="❌"
          
          IS_CLEAN="${{ github.event.inputs.force_clean_build == true && 'Yes' || 'No' }}"
          DO_REL="${{ github.event.inputs.create_release == true && 'Yes' || 'No' }}"
          
          cat << EOF > tg_message.txt
          $ICON <b>Kernel Build ${STATUS^^}</b>
          ━━━━━━━━━━━━━━━━━━━━━
          📱 <b>Target:</b> Pixel 8 / 8a / 8 Pro
          🛠️ <b>Variant:</b> backslashxx (KernelSU)
          ⚙️ <b>Features:</b> atp4pixel Pillars + BTF
          🧹 <b>Clean Build:</b> $IS_CLEAN
          📦 <b>Release Created:</b> $DO_REL
          👤 <b>Triggered by:</b> <a href="https://github.com/${{ github.actor }}">${{ github.actor }}</a>
          
          🔗 <a href="https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}">View Run Logs & Artifacts</a>
          EOF
          
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
            -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
            -d text="$(cat tg_message.txt)" \
            -d parse_mode="HTML" \
            -d disable_web_page_preview=true
