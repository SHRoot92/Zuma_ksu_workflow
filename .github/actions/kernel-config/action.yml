name: 'Inject Kernel Configs'
description: 'Applies standard networking, performance, security, and KSU flags'
inputs:
  defconfig_path:
    description: 'Path to the defconfig or .config file'
    required: true
  hook_mode:
    description: 'Hook mode (manual, lsm)'
    required: false
    default: 'manual'
  enable_susfs:
    description: 'Set to "true" to inject SuSFS configs'
    required: false
    default: 'false'

runs:
  using: "composite"
  steps:
    - name: Apply Configurations
      shell: bash
      run: |
        DEFCONFIG="${{ inputs.defconfig_path }}"
        if [ ! -f "$DEFCONFIG" ]; then
          echo "Error: Defconfig file not found at $DEFCONFIG"
          exit 1
        fi

        cat >> "$DEFCONFIG" <<-'EOF'
        # Core & Security Base
        CONFIG_AUDIT=y
        CONFIG_TMPFS_XATTR=y
        CONFIG_TMPFS_POSIX_ACL=y
        CONFIG_LOG_BUF_SHIFT=21
        CONFIG_SECURITY_NETWORK=y
        CONFIG_CGROUPS=y
        CONFIG_CGROUP_BPF=y
        CONFIG_CGROUP_SCHED=y
        CONFIG_CGROUP_PIDS=y
        CONFIG_CGROUP_FREEZER=y
        CONFIG_INOTIFY_USER=y
        CONFIG_BPF_LRU_MAP=y
        CONFIG_DEBUG_INFO_BTF=y
        CONFIG_BPF_UNPRIV_DEFAULT_OFF=y

        # Networking Configuration (Fortress Deep Unlock)
        CONFIG_NETFILTER_ADVANCED=y
        CONFIG_IP_NF_TARGET_TTL=y
        CONFIG_IP6_NF_TARGET_HL=y
        CONFIG_IP6_NF_MATCH_HL=y
        CONFIG_BPF_STREAM_PARSER=y
        CONFIG_NETFILTER_XT_MATCH_ADDRTYPE=y
        CONFIG_NETFILTER_XT_TARGET_TPROXY=y
        CONFIG_NETFILTER_XT_TARGET_REDIRECT=y
        CONFIG_NETFILTER_XT_MATCH_SOCKET=y
        CONFIG_NETFILTER_XT_MATCH_MARK=y
        CONFIG_NETFILTER_XT_MATCH_IPRANGE=y
        CONFIG_NETFILTER_XT_MATCH_OWNER=y
        CONFIG_NETFILTER_XT_MATCH_MAC=y
        CONFIG_NETFILTER_XT_MATCH_CONNTRACK=y
        CONFIG_NETFILTER_XT_MARK=y
        CONFIG_NETFILTER_XT_CONNMARK=y
        CONFIG_NETFILTER_XT_TARGET_CONNMARK=y
        CONFIG_IP6_NF_NAT=y
        CONFIG_IP6_NF_TARGET_REDIRECT=y
        CONFIG_IP6_NF_TARGET_MASQUERADE=y
        CONFIG_IP_MULTIPLE_TABLES=y
        CONFIG_IPV6_MULTIPLE_TABLES=y
        CONFIG_XFRM_ALGO=y
        CONFIG_INET_IPCOMP=y
        CONFIG_INET_XFRM_TUNNEL=y
        CONFIG_INET_ESP=y
        CONFIG_XFRM_USER=y
        CONFIG_XFRM_SUB_POLICY=y
        CONFIG_INET_XFRM_MODE_TUNNEL=y
        CONFIG_XFRM_STATISTICS=y

        # IPSet Suite (64k)
        CONFIG_IP_SET=y
        CONFIG_NETFILTER_XT_SET=y
        CONFIG_IP_SET_MAX=65534
        CONFIG_IP_SET_BITMAP_IP=y
        CONFIG_IP_SET_BITMAP_IPMAC=y
        CONFIG_IP_SET_BITMAP_PORT=y
        CONFIG_IP_SET_HASH_IP=y
        CONFIG_IP_SET_HASH_IPMARK=y
        CONFIG_IP_SET_HASH_IPPORT=y
        CONFIG_IP_SET_HASH_IPPORTIP=y
        CONFIG_IP_SET_HASH_IPPORTNET=y
        CONFIG_IP_SET_HASH_IPMAC=y
        CONFIG_IP_SET_HASH_MAC=y
        CONFIG_IP_SET_HASH_NETPORTNET=y
        CONFIG_IP_SET_HASH_NET=y
        CONFIG_IP_SET_HASH_NETNET=y
        CONFIG_IP_SET_HASH_NETPORT=y
        CONFIG_IP_SET_HASH_NETIFACE=y
        CONFIG_IP_SET_LIST_SET=y

        # TCP Congestion Control
        CONFIG_TCP_CONG_ADVANCED=y
        CONFIG_TCP_CONG_BBR=y
        CONFIG_NET_SCH_FQ=y
        CONFIG_DEFAULT_BBR=y
        EOF

        # Hook Mode Resolution
        HOOK_MODE="${{ inputs.hook_mode }}"

        if [ "$HOOK_MODE" = "manual" ]; then
          cat >> "$DEFCONFIG" <<-'EOF'
        CONFIG_KSU=y
        CONFIG_KSU_LSM_SECURITY_HOOKS=n
        CONFIG_KSU_HOSTSREDIRECT=y
        EOF
        elif [ "$HOOK_MODE" = "lsm" ]; then
          cat >> "$DEFCONFIG" <<-'EOF'
        CONFIG_KSU=y
        CONFIG_KSU_LSM_SECURITY_HOOKS=y
        CONFIG_KSU_HACK_ARM64_BRANCH_LINK=y
        CONFIG_KSU_THRONE_TRACKER_ALWAYS_THREADED=y
        CONFIG_KSU_HOSTSREDIRECT=y
        EOF
        fi

        # SuSFS Injection
        if [ "${{ inputs.enable_susfs }}" = "true" ]; then
        cat >> "$DEFCONFIG" <<-'EOF'
        CONFIG_KSU_SUSFS=y
        CONFIG_KSU_SUSFS_HAS_MAGIC_MOUNT=y
        CONFIG_KSU_SUSFS_SUS_PATH=y
        CONFIG_KSU_SUSFS_SUS_MOUNT=y
        CONFIG_KSU_SUSFS_AUTO_ADD_SUS_KSU_DEFAULT_MOUNT=y
        CONFIG_KSU_SUSFS_AUTO_ADD_SUS_BIND_MOUNT=y
        CONFIG_KSU_SUSFS_SUS_KSTAT=y
        CONFIG_KSU_SUSFS_TRY_UMOUNT=n
        CONFIG_KSU_SUSFS_AUTO_ADD_TRY_UMOUNT_FOR_BIND_MOUNT=n
        CONFIG_KSU_SUSFS_SPOOF_UNAME=y
        CONFIG_KSU_SUSFS_ENABLE_LOG=y
        CONFIG_KSU_SUSFS_HIDE_KSU_SUSFS_SYMBOLS=y
        CONFIG_KSU_SUSFS_SPOOF_CMDLINE_OR_BOOTCONFIG=y
        CONFIG_KSU_SUSFS_OPEN_REDIRECT=y
        CONFIG_KSU_SUSFS_SUS_MAP=y
        CONFIG_KSU_FEATURE_SULOG=y
        CONFIG_KSU_FEATURE_ADBROOT=y
        EOF
        fi
